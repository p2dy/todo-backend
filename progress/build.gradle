plugins {
    id 'io.quarkus'
    id 'todo-backend.conventions'
    id 'org.kordamp.gradle.jandex' version '0.11.0'
}

dependencies {
    implementation 'io.quarkus:quarkus-smallrye-graphql:2.7.0.Final'

    implementation(project(':core'))
    testImplementation(testFixtures(project(":core")))
}